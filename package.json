{
  "name": "crossword-grid-formatter",
  "version": "0.1.0",
  "description": "Normalizes messy crossword grid text into a canonical form",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "tsc",
    "start": "node dist/cli.js",
    "test": "tsc && node --test dist"
  }
}
